#include <stdio.h>
 
int main() {
 
    /**
     * Escreva a sua solução aqui
     * Code your solution here
     * Escriba su solución aquí
     */
     int a, b;
     float c;
    scanf("%d%d%f", &a, &b, &c);
    printf("NUMBER = %d\nSALARY = U$ %.2f\n", a, b * c);
 
    return 0;
}
